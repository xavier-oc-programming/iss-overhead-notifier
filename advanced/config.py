# API / URLs
ISS_API_URL = "http://api.open-notify.org/iss-now.json"
SUNRISE_SUNSET_API_URL = "https://api.sunrise-sunset.org/json"

# Location
MY_LAT = 40.416775
MY_LONG = -3.703790
OVERHEAD_THRESHOLD = 5  # degrees — ISS must be within this range to count as overhead

# Email / SMTP
SMTP_HOST = "smtp.gmail.com"
SMTP_PORT = 587

